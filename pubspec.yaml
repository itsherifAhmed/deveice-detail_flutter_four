name: deviceinfo
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  device_info: ^0.4.0+4
  flutter_launcher_icons:

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter_icons:
  image_path: "snapshot/info.png"
  android: true
  ios: true

flutter:

  uses-material-design: true
